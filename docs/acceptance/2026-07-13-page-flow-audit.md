# 人资系统页面与流程巡检（2026-07-13）

## 本次结论

- 13 个自定义 Desk 页面均可由 Frappe 实际加载：工作台、组织架构、人员档案/花名册/导入导出/报表/任职记录、考勤导入中心、薪资中心、设置中心与团队动态。
- 所有 30 个 `tests/verify_*.js` 契约测试通过；Python 语法检查与 `git diff --check` 通过。
- 未创建、修改或删除永新公司的员工、考勤、薪资或钉钉业务数据。

## 已修复的阻断项

| 模块 | 问题 | 修复后的逻辑 |
| --- | --- | --- |
| 人事 / 任职记录 | 侧栏直达时可能提示“页面 employee-property-history 未找到” | 迁移完成后统一注册人员自定义页面，不再依赖用户先点击顶部导航；本地已迁移并验证页面加载。 |
| 人事 / 任职记录 | 页面已打开但读取记录时报“检查 Employee Transfer / Employee Promotion 权限” | 根因是 Frappe RPC 强制要求白名单方法参数带类型标注；补全接口契约后，浏览器实际显示 TEST-MOV-007 的转正、异动、原值/新值与来源单据。 |
| 全局公司 | 各页面可能各自使用用户默认公司，导致组织、薪酬等上下文不一致 | 顶部公司选择器默认优先永新并向页面广播当前公司；组织架构和薪酬中心会随之刷新。 |
| 考勤 | 顶部当前公司为永新，但考勤页局部公司输入框仍显示旧值 `1`，可造成导入和月度汇总错位 | 考勤页改为订阅全局公司上下文、等待初始化完成并只读显示当前公司；浏览器刷新后已显示永新。 |
| 薪酬 | 公司可手工输入为 `1` 等任意文本，容易跨公司读取/生成 | 公司字段改为只读显示，唯一切换入口是顶部全局公司选择器。 |
| 钉钉 | 默认启用公网网关，与一期“导出表导入”方案冲突 | 默认改为“Excel导入（默认）”；本地/公网网关、API 集成均关闭，保留后续服务器上线时显式启用的能力。 |
| 自定义接口 | 部分白名单接口同样可能因未标注参数类型而被 Frappe 返回 417 | 新增全量接口类型契约测试，并补齐考勤种子、薪资查询、钉钉配置及同步入口的参数标注。 |

## 界面交互范围

已验证的是服务端真实页面加载、页面脚本和业务接口契约，以及本机 Chrome 的只读界面交互：

- 任职记录：页面打开、测试记录读取、来源字段、来源单据按钮与无下一页状态。
- 组织架构：全局公司为永新，组织树、编制/现有人数和负责人匹配信息正常读取。
- 考勤工作台：全局公司同步到局部只读字段；未上传或导入文件。
- 薪酬管理中心：全局公司同步到局部只读字段，薪资规则列表正常读取。
- 招聘：首页、职位空缺、申请人、面试、录用通知、聘书及面试反馈入口均可读取，未创建候选人或面试记录。

未执行新增、导入、删除、锁定/解锁考勤、生成薪资结算、发送工资条或任何会写入永新业务数据的按钮。

仍需在下一轮针对招聘、审批、培训、绩效的真实业务表单进行单独验收；这些模块不应使用 TEST 数据替代真实审批规则、面试模板或考核周期配置。
