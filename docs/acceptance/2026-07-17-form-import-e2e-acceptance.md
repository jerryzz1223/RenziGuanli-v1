# 人资表单端到端验收（TEST-HRMS）

验收日期：2026-07-17
验收标识：`TEST-FORM-E2E-20260717-V8`
执行范围：仅 `TEST-HRMS`；永新及公司 `1` 未写入。

## 结论

通过。28 类表单均完成模板下载、填写、预校验与导入；其中 27 类暂存表单全部完成审批、正式单据生成、提交/确认生效和目标字段状态核验。员工花名册通过专用智能导入写入一名测试员工并校验在职状态。

## 验收链路

```text
下载模板 -> 填写测试行 -> 上传预校验 -> 导入暂存
-> 人事/财务审批 -> 生成正式草稿 -> 提交或确认生效 -> 校验目标记录
```

## 结果

| 项目 | 结果 |
| --- | --- |
| 表单总数 | 28 |
| 直接花名册导入 | 1/1 通过 |
| 暂存型表单下载、填写、预校验、导入 | 27/27 通过 |
| 审批、生成正式单据、提交/确认生效 | 27/27 通过 |
| 页面模板入口 | 28 张模板卡片可见 |
| 受保护公司数据 | 永新 205 人、公司 `1` 0 人，验收前后不变 |

## 关键业务验证

- 薪资资料走两级审批：人事复核后才能进入财务复核。
- 月度考勤终稿先确认，再锁定考勤月份；薪资来源记录随后才允许生成与确认。
- 离职资料使用已提交的假期表分配作为系统前置条件，已成功提交离职单。
- 奖惩、证书多能工、离职薪资结算等薪资来源字段已与福利扣款来源的可选值契约对齐。
- 组织、合同、证书、部门考勤、提案、系统反馈均生成可追溯的正式业务记录。

## 可重复执行

```bash
bench --site hrms.localhost execute hrms.api.form_import_e2e_acceptance.run_form_import_e2e_acceptance
```

验收入口实现：`hrms/api/form_import_e2e_acceptance.py`。
