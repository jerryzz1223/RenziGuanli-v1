# 组织配置导入导出

入口：部门 → 部门列表或组织架构图 → 顶部“组织配置” → 导出配置／导入配置。只有系统管理员可以操作整个公司的配置迁移。

## 迁移到服务器

1. 服务器先部署包含 `hrms.api.organization_package` 的系统版本，并更新缓存。持续自动更新还需要启用 Frappe 调度器和后台队列；新版本的 Employee hooks 和定时任务需随应用部署注册。
2. 在服务器导入花名册，核对工号、部门、岗位。有效导入行中的新部门会在确认导入时创建；预览仅列出待创建部门，不写入数据。停用部门或跨公司冲突仍需处理。
3. 在已经整理好的本地系统选择“导出配置”，获得私有 `.xlsx` 文件。
4. 在服务器选择“导入配置”，选择目标公司并上传文件，点击“校验并预览”。
5. 核对新增／更新数量、上下级、职级和待确认事项。修正错误后重新上传。预览通过后点击“确认导入”。
6. 列表与树图读取同一份配置。勾选“导入后按花名册自动更新人员”时，后续花名册变动会更新自动节点；手工节点保留手工安排。

## 文件内容

| 工作表 | 用途 |
| --- | --- |
| 组织层级 | 每个节点一行；节点编号保持不变，上级节点编号决定上下级。上级为空表示选定公司的直属节点。关联部门使用部门名称，服务器按目标公司唯一匹配。 |
| 人员任职 | 节点编号、引用类型、工号、姓名、图中职务、负责人位置、仅展示代理。工号按文本填写，保留前导零。 |
| 职级定义 | 职级编码、名称、等级顺序、上级职级编码。等级顺序越小越高；不清楚的等级可留空。组织节点的“职级编码”引用这里。 |
| 说明 | 格式版本及填写规则；请保留表头和格式版本。 |

“上级节点编号”与“上级职级编码”含义不同：前者决定谁在谁下面，后者定义等级体系。例如某岗位填 M2，不会自动改变它所属的课或组。岗位名、课长／组长等称呼不足以确定具体汇报关系。

示例（仅说明格式，不代表永新的实际职级）：

| 职级编码 | 职级名称 | 等级顺序 | 上级职级编码 |
| --- | --- | --- | --- |
| M1 | 管理职级 | 1 | |
| M2 | 岗位职级 | 2 | M1 |

导入定义后，编辑组织节点时可选择“图中职级”；列表与树图显示该职级。这里不会修改员工档案的 Employee Grade。

## 人员与代理

- 只按工号匹配目标公司的在职员工，不使用姓名或来源服务器内部 ID 猜测身份。
- 工号不存在、重复、员工已离职或部门不一致时提示错误；修正文件或服务器花名册后再导入。
- 原表人员缺少工号时可以保留姓名注释，显示待确认，不计入实际人数，也不会按同名自动绑定。
- 引用类型“代理人”保留代理身份；原表人员的负责人位置可以填 `primary` 或 `proxy`。已经确认的跨部门代理可标记“仅展示代理”。
- 自动普通岗位按部门和岗位分配；原表组线按已经绑定的工号更新。花名册只有部门和职位时，无法判断同名生产组、IPQC 组的具体归属，需要先导入或手工定义。
- “合并关联部门”每行一个部门名称，使独立花名册部门进入图中已经确认的分组，避免重复建根节点。
- 普通自动岗位迁移部门／岗位匹配规则，导入时按目标服务器的在职花名册重新匹配；来源机器的自动岗位成员快照不作为固定任职。旧配置中的这类快照也兼容处理。手工任职、负责人、代理及原表组线绑定仍按工号严格校验，已固定安排的人员不会被普通自动岗位重复占用。
- 人员的部门校验包含本节点及上级明确配置的合并关联部门。只有显式配置的合并范围可以跨部门匹配；不会根据相似名称自行合并，也不会改写员工档案的部门。

## 更新规则

- 相同节点编号更新原节点，缺少的编号新增；文件未包含的节点通常保留。明确的合并关联部门若对应服务器已有的空独立节点，预览会列出并在导入时停用该空节点；有人员、任职、编制或下级时阻止自动合并，要求先核对。不要为了改名称而改节点编号。
- 不删除或覆盖 Employee、Department、Designation 或 Employee Grade 基础资料。
- 导入前生成私有配置备份，并保留 Frappe 节点修改记录。备份用于恢复对应节点的配置；因为导入采用合并模式，恢复备份不会自动删除后来新增的节点。
- 节点循环、职级循环、缺少上级、重复部门节点、重复自动岗位、无效字段、文件公式均在导入前校验。
- 预览后花名册或组织已变化，需要重新预览；防止用旧预览覆盖新数据。
- 文件及备份包含人员信息，均保存为私有附件。

## 本次验证范围

在本地 Frappe 环境测试真实配置导出／导入回滚、199 人显示一致、重复导入、代理、前导零工号、来源节点编号与服务器内部编号不同、两级职级、手工编辑保留、错误校验与权限。

2026-09-09 已通过登录后的 Chrome 页面执行配置选择、预览、确认导入和导出：新增 0、更新 191 个节点，418 条人员引用；导入前备份入口可见。导入后逐员工 ID 核对 199 名在职人员，连续课 82 人，业务组归属总办室，没有重新出现公司直属的空业务组。19 个无工号的原表姓名作为待确认注释保留。

以上初次验收时内网服务器尚未部署；后续进展见下方日期记录。交付文件中的职级定义为空；现有资料没有正式职级编码及等级顺序，不根据职位名称虚构职级。

### 2026-09-09 后续服务器修复验证

已将组织配置相关 10 个运行文件以校验哈希、备份后替换的方式更新到内网服务器；未升级其他业务模块。服务器 Chrome 对原先失败的 190 节点配置重新预览通过，可点击“确认导入”，原工号 260501 的两条部门不一致错误消失，普通自动岗位按目标花名册匹配的提示可见。这证明更新后的导入校验已在服务器运行，但尚不代表配置已写入。

最新本地迁移配置包含 192 个节点、407 条固定人员引用、19 条无工号引用及 17 个带原表职级标签的节点；普通自动岗位另按目标花名册匹配。截止此次记录，Chrome 文件访问权限仍未通过自动选文件检查，最新文件的服务器上传、正式导入及树图验收尚待完成。服务器已有在职 192 人，本地为 200 人，迁移后人数应以服务器花名册为准。


### 2026-09-10 花名册部门与组织配置流程

- 新部门在有效花名册行确认导入时按公司、部门名称识别和创建；预览列出创建计划。同部门重复行复用，失败行回滚部门创建，避免空资料残留。
- “部门基础资料”改名为“花名册部门”，保留花名册归属资料及人数核对；“查看配置关系”展示其对应的组织路径和合并关系。旧 Department 上级字段属于基础挂接，不代表图中汇报关系；表单移除另一套层级、负责人编辑入口，统一转向组织配置。部门列表和架构图仍共用 Organization Node。
- 组织配置菜单和导出结果增加原表工号核对入口，展示同名候选档案工号、部门、在职状态，并可进入任职核对；姓名相同不能作为自动绑定依据。已绑定的卡片人员显示工号。
- 本地已备份并停用无人员、无下级、无编制的重复 QE 组节点；原表 QE 组显式关联花名册 QE 组部门。导入器也支持预览并停用目标端对应的空独立节点，有人员或既有安排时阻止自动处理。
- 本地当前导出为 191 个节点、407 条人员引用、19 条未绑定引用、17 个原表职级标签节点。19 条中有 16 条同名离职候选、2 条无同名候选、1 条同名在职兼任待核对；这些是候选，不是已确认身份。
- 本地在职总数仍为 200，其中 13 人的花名册部门为空，继续显示为待完善部门，不能通过配置猜测其归属。未改写员工档案。
- Chrome 已验收组织图工号显示、单个 QE 组、导出及工号核对表、花名册部门关系表与资料表单；Safari 已验收本地组织图。实际 Frappe 导入回滚检查覆盖新部门复用、失败行回滚、配置往返、空节点合并及重复导入，18 项组织单元测试和相关前端静态检查通过。
- 本轮新增修改尚未部署到内网服务器，服务器最新配置上传、正式导入与页面验收仍待完成；此前服务器预览通过不等于本轮部署完成。

## 导出树状架构图

入口：组织架构图 → 顶部“导出架构图”。

- **Excel 可编辑架构图（默认）**：按当前系统组织树生成合并单元格、文字、框线与上下级连线，不嵌入截图。完整导出附顶层课室分表，当前层级只生成选定范围。保留全部卡片人员及正式、代理、待确认标记。字体 11 磅、长内容逐行展开，打印按 100% 分页，不强制压成一张纸。可在 Excel 中编辑文字与边框；重新导出会按系统最新数据生成新文件，Excel 内修改不会回写系统。
- **完整架构**：导出当前公司整棵展示树，包含折叠的全部下级和卡片人员；忽略页面缩放、滚动、搜索与局部查看范围。
- **当前展开层级**：保留当前查看范围、搜索结果与展开状态，收起的下级不进入图片。
- **SVG 矢量图**：建议用于完整架构，可在浏览器打开、放大查看和打印。包含独立文字、卡片和连线，不依赖系统页面样式。
- **PNG 图片**：便于分享；整图等比缩放到最长边 8192 像素、最多 1600 万像素以内，大图查看细节建议使用 SVG。

导出使用当前已加载的组织数据，保留代理、正式与待确认等卡片标记，不改变任职、人数或展开状态。文件通过浏览器下载。图片用于查看；服务器迁移仍使用“组织配置”导出的 XLSX 文件。

2026-09-09 本地登录 Chrome 验证：完整 SVG 与 PNG 下载成功，完整图 122 个展示节点，与页面展开全部一致；收起后的当前层级 SVG 为 3 个节点。下载 SVG 已核对 XML、节点及文字边界，PNG 为 8192×1877 像素；功能检查涵盖全部人员、同人多处任职、搜索范围、文本转义和导出不修改数据。展示节点数与配置记录数口径不同，停用、不导入或未进入当前展示树的配置记录不会出现在图片中。内网服务器尚未部署验收。

Excel 导出已在本地登录页面生成并提供直接下载链接：122 个节点，完整图和 11 个课室分表，共 12 张。已读取保存后的工作簿核对全部人员文字、同人多处任职、代理、连续课 82 人、长名单、无重叠合并区域、无图片嵌入、文本公式注入防护和导出权限，并渲染检查全部工作表。服务器通过现有 openpyxl 依赖生成，不依赖电脑上的原始 Excel 模板或桌面表格工具。部署时须包含 `hrms.api.organization_chart_export` 新模块及更新后的页面 JS；图形工作簿与可迁移组织配置仍是两种文件。
